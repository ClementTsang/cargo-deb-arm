name: "cargo deb"
description: "Executes cargo deb for aarch64"
inputs:
  args:
    description: "Arguments to pass to cargo deb"
    required: true
    default: ""
runs:
  using: "docker"
  image: "Dockerfile"
  args:
    - ${{ inputs.args }}
